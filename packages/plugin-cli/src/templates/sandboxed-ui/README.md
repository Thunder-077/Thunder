# __PLUGIN_NAME__

Sandboxed Thunder desktop plugin. Run `pnpm dev` while Thunder Desktop is running.

This template has no local runtime. Add only the permissions required by the
Host Bridge APIs used by the plugin.
